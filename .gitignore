# Sensitive files — KHÔNG commit
.env
.env.local
.env.production
server/bot_state.json
server/state.json
openclaw.json
*.pem
*.key

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

openclaw.local.json
*.bak
test-live-execute.ts
test-live-borrow.ts
test-live-repay.ts
.memwal_account.json
.memwal_store/

# Agent binaries — distribute via GitHub Releases, NOT committed to the repo
*.exe
dist-agent/

# Electron desktop build output (portable .exe ~110MB) — distribute via Releases
release/
release-build/

# Python agent virtualenv (huge) + caches
agent/.venv/
**/__pycache__/
*.pyc

# Self-signed dev certs (the *.key is already ignored above)
certs/

# Research datasets (large raw klines / sweep outputs; regenerate via server/fetch_*.ts).
# NOTE: public/data/ IS kept — the frontend backtest loads it at runtime.
server/data/

# Scratch
*.output

# Local-only dev/test/research files — gathered here, never pushed to git
_dev/
scratch/
scratch_docs/
# Defensive: standalone test/check/debug/sweep/fetch scripts (run via `npx tsx`)
server/test_*.ts
server/check_*.ts
server/debug_*.ts
server/sweep_*.ts
server/fetch_*.ts
server/publish_template_bots.ts
server/skills_draft/
server/execute_agent_test.ts
server/*_test.ts
# Root-level scratch test scripts (run via `npx tsx` / node) — never committed
/test-*.ts
/test-*.cjs
/test.js
/final-test.cjs
test-e2e-*.cjs
fetch_docs.js
